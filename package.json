{
  "name": "monster-spil",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev": "npm run dev -w client -- --host",
    "dev:server": "npm run dev -w server",
    "build": "npm run build -w shared && npm run build -w client",
    "build:server": "npm run build -w server",
    "typecheck": "tsc -b",
    "test": "npm run build -w shared && node --test shared/dist/**/*.test.js",
    "add-creature": "echo 'add-creature script: not implemented yet (Milestone 2+)' && exit 1"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
